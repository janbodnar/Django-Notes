# Database 


## PostgreSQL setup

```python
DATABASES = {
    'default': {
        'ENGINE': 'django.db.backends.postgresql',
        'HOST': 'localhost',
        'NAME': 'testdb',
        'USER': 'postgres',
        'PASSWORD': 'andrea',
    }
}
```
